[project]
name = "tensorflow-learn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [{ name = "Huayu Zhang", email = "zhanghuayu.dev@gmail.com" }]
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.5.2",
    "tensorflow>=2.21.0",
    # "tf-nightly"
]

[project.scripts]
tensorflow-learn = "tensorflow_learn:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
